apiVersion: v1
kind: Service
metadata:
  labels:
    app: udagram-ui
  name: udagram-ui
  namespace: default
  selfLink: /api/v1/namespaces/default/services/udagram-ui
spec:
  ports:
  - port: 4200
    protocol: TCP
    targetPort: 4200
  selector:
    app: udagram-ui
  sessionAffinity: None
  type: ClusterIP
status:
  loadBalancer: {}
